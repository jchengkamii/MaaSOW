# MXU macOS 版验证记录

- 使用官方 MXU 2.6.1、MaaFramework 5.12.3，官方 Apple Silicon/Intel 发布归档已下载；4 个文件均匹配 GitHub 发布资产的 SHA-256。
- 完整回归：146 项，145 项通过，1 项跳过（未附带本地 MXU 用户配置）。详细构建日志位于工作副本 test-results.txt。
- 两种架构均已实际解压安装到临时目录，检查 MXU 和必需框架库的 Mach-O 架构、运行文件哈希、许可证；没有在 Windows 上运行 Mac 二进制。
- 已测试归档路径越界阻止、损坏归档校验、运行文件损坏识别、架构选择，以及 MXU 退出后的自动帮助清理。
- 已测试 MXU 和 Agent 拒绝 GlobalEvent，Agent 创建控制器时固定传入 PostToPid；后台帮助沿用同一个控制器工厂。
- 已保留现有业务/离线截图识别、PID 身份验证、真实子进程停止与跨进程互斥测试。
- Python 编译与三个 .command 脚本的 Bash 语法检查通过；已移除 Tk 前端依赖。

已检查 MaaFramework v5.12.3 的 PostToPidInput.mm：输入使用 CGEventPostToPid，不调用全局输入或窗口激活 API。本项目不会回退到 GlobalEvent。

**尚未进行 Mac 真机验证**：MXU GUI 启动、系统授权、fcntl 锁、微信后台事件响应、是否自行激活以及各任务实际成功率仍待验证。应用自行抢焦点的行为不受输入投递方式完全控制。先运行检查环境脚本，然后在 MXU 试运行简单任务。
